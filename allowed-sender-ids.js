// Should be a .json file instead for simplicity,
// but node's ESM can't import JSON files without an additional experimental flag
// (which I haven't figured out how to add it in vercel's node runtime)

export default [
    '1001075466164293632'
];
